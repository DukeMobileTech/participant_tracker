<div class="row">
    <div class="col-md-12">
        <h2 class="page-title">Participant Property Label: <%= @participant_attribute.value %></h2>
    </div>
</div>
<section class="widget">
<p id="notice"><%= notice %></p>

<p>
  <strong>Participant ID:</strong>
  <%= @participant_attribute.participant_id %>
</p>

<p>
  <strong>Property ID:</strong>
  <%= @participant_attribute.property_id %>
</p>

<p>
  <strong>Value:</strong>
  <%= @participant_attribute.value %>
</p>

<%= link_to 'Edit Property', edit_participant_participant_attribute_path(@participant, @participant_attribute), class: 'btn btn-primary' %> 
</section>
