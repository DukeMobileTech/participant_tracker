<section class="widget">                        

<%= form_for(@relationship_type) do |f| %>
  <% if @relationship_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@relationship_type.errors.count, "error") %> prohibited this relationship_type from being saved:</h2>

      <ul>
      <% @relationship_type.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="row control-group">
    <label class="col-md-4 control-label"> <%= f.label :participant_type_owner_id %> </label>
    <div class="col-md-8 controls form-group"> <%= f.select(:participant_type_owner_id, ParticipantType.all.collect {|p| [p.label, p.id]}) %> </div>
  </div>
  <div class="row control-group">
    <label class="col-md-4 control-label"> <%= f.label :participant_type_related_id %> </label>
    <div class="col-md-8 controls form-group"> <%= f.select(:participant_type_related_id, ParticipantType.all.collect {|p| [p.label, p.id]}) %> </div>
  </div>
  <div class="row control-group">
    <label class="col-md-4 control-label"> <%= f.label :label %> </label>
    <div class="col-md-8 controls form-group"> <%= f.text_field :label %> </div>
  </div>
  
  <div class="form-actions">
    <%= f.submit class: 'btn btn-primary' %>
  </div>
<% end %>

</section>
